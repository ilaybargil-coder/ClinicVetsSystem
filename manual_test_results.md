# 🧪 תוצאות בדיקה — אימות מול הקוד (ClinicVets)

> **שיטה:** מעבר על כל שורה בצ'קליסט מול קוד המקור (לא הרצה חיה).
> **מקרא:** ✅ הקוד תומך כצפוי · ❌ באג / לא תואם · 👁 דורש בדיקת-עין חיה (תצוגה/צבע/מייל)
> תאריך: 09/06/2026

---

## ✅ עדכון 10/06/2026 — כל הבאגים תוקנו ואומתו בבדיקה חיה מול Supabase

> אומת בהרצת Insert/Update/Delete אמיתיים מול מסד הנתונים (6/6 בדיקות עברו, רשומות הבדיקה נוקו).

1. **באג #1 (הוספת חיה) — תוקן, אבל האבחנה למטה שגויה!** הסיבה האמיתית **אינה** DateOnly (Newtonsoft 13.0.3 תומך בו). הכשל היה המאפיין `NeedsVaccine` ב-[Models/Pet.cs](ClinicVetSystem/Models/Pet.cs) שנשלח ל-DB כעמודה לא קיימת. תוקן עם `[JsonIgnore]`. הוספת חיה עובדת משני המסלולים.
2. **באג נוסף שהתגלה בבדיקה החיה: סימון תרופה בביקור נכשל** — ב-[Models/VisitMedication.cs](ClinicVetSystem/Models/VisitMedication.cs) ה-PK הוגדרו `shouldInsert: false` ⇒ נשלח JSON ריק. תוקן ל-`true`. שמירת רשומה עם תרופות עובדת.
3. **באג #2 (מלאי לא יורד) — מומש**: שמירת ביקור מורידה מלאי ב-1 לכל תרופה שנוספה (ומחזירה +1 לתרופה שהוסרה) — [VisitsView.axaml.cs](ClinicVetSystem/VisitsView.axaml.cs).
4. **באג #3 (ולידציה חסרה) — הושלם**: הוספת חיה מפרופיל לקוח בודקת עכשיו שם/משקל/תאריכים/שבב בדיוק כמו המסך הראשי — [CustomersView.axaml.cs](ClinicVetSystem/CustomersView.axaml.cs).
5. **באג חדש שהתגלה ותוקן: עריכת חיה בלי שבב/חיסון** — `Set()` עם null מפיל את postgrest; מסלול העריכה הוסב ל-Update מבוסס-מודל — [PetsView.axaml.cs](ClinicVetSystem/PetsView.axaml.cs).

---

## 🔴 3 ממצאים קריטיים (קודם כל אלה)

### באג #1 — הוספת חיה נכשלת (הפדיחה מההצגה)
- **קובץ:** [Models/Pet.cs:26](ClinicVetSystem/Models/Pet.cs) — `BirthDate` הוא `System.DateOnly`.
- **סיבה:** המערכת על `postgrest-csharp 3.5.1`, שלא יודעת לסרל נכון `DateOnly` ב-`Insert`. הערך נשלח כאובייקט במקום כתאריך, ו-Postgres דוחה.
- **ראיה חזקה:** מכל המודלים — `Staff`, `Customer`, `Visit`, `Medication` — **רק `Pet` משתמש ב-`DateOnly`**. כולם נשמרים תקין; רק חיה מתפוצצת. (הרשמת עובד עובדת = ההוכחה.)
- **משפיע על שני המסלולים:** [PetsView.axaml.cs:242](ClinicVetSystem/PetsView.axaml.cs) וגם [CustomersView.axaml.cs:344](ClinicVetSystem/CustomersView.axaml.cs).
- **תיקון:** או JsonConverter ל-`DateOnly` (שומר ISO `yyyy-MM-dd`), או שינוי המודל ל-`DateTime`.

### באג #2 — מלאי התרופה לא יורד אחרי ביקור
- **קובץ:** [VisitsView.axaml.cs:437](ClinicVetSystem/VisitsView.axaml.cs) (`BtnSaveRecord_Click`).
- הקוד שומר `visit_medications` ומחשב עלות, אבל **אף פעם לא מפחית `medications.stock`**.
- הצ'קליסט מצפה לזה ב-Part 5 וב-Part 8 → **לא מומש בכלל**.

### באג #3 — "הוספת חיה מפרופיל לקוח" כמעט בלי ולידציה
- **קובץ:** [CustomersView.axaml.cs:302](ClinicVetSystem/CustomersView.axaml.cs) (`BtnSaveAddPet_Click`).
- בודק רק: שם לא ריק + סוג נבחר. **חסר:** שם אותיות-בלבד, טווח משקל 0.1–100, תאריך לידה (עתידי/לפני 2000), שבב 15 ספרות.
- לעומת זאת המסך הראשי [PetsView.axaml.cs:169](ClinicVetSystem/PetsView.axaml.cs) **כן** בודק הכל. → **חוסר עקביות בין שני המסכים.**

---

## 🔐 חלק 1 — התחברות ורישום

### התחברות — [MainWindow.axaml.cs:67](ClinicVetSystem/MainWindow.axaml.cs)
| בדיקה | תוצאה | הערה |
|---|:--:|---|
| פרטים נכונים → נכנס | ✅ | פותח MainMenuWindow |
| סיסמה שגויה → שגיאה אדומה | ✅ | "שם משתמש או סיסמה שגויים" + צבע #ba1a1a |
| שדה ריק → שגיאה | ✅ | נבדק לפני פנייה ל-DB |
| מוצג שם המשתמש אחרי כניסה | 👁 | בדשבורד `lblTopName=Username` — ודא בעין |

### רישום שם משתמש — [MainWindow.axaml.cs:123](ClinicVetSystem/MainWindow.axaml.cs)
`^[a-zA-Z0-9]{6,8}$` + מקס' 2 ספרות
| `ab` קצר → ❌שגיאה | ✅ | `abcdefghi` ארוך → ❌שגיאה | ✅ |
|---|:--:|---|:--:|
| `abc123d` (3 ספרות) → ❌שגיאה | ✅ | `abc12de` → **עובר** | ✅ |

### רישום סיסמה — [MainWindow.axaml.cs:127](ClinicVetSystem/MainWindow.axaml.cs)
8–10 תווים, אות, **בדיוק** ספרה אחת, **בדיוק** תו מיוחד אחד מ-`$#!`
| כל מקרי הצ'קליסט (קצרה/ארוכה/בלי ספרה/בלי מיוחד/2 ספרות/2 מיוחדים) | ✅ | תואמים |
|---|:--:|---|
| `Abcdef1!` → **עובר** | ✅ | |
| ⚠ הערה | ❌ | אין בדיקה שכל התווים חוקיים — תו כמו `@` יחמוק (לא בצ'קליסט) |

### רישום שאר השדות
| ת"ז ≠ 9 ספרות → שגיאה | ✅ | [:133](ClinicVetSystem/MainWindow.axaml.cs) |
|---|:--:|---|
| מייל בלי @ / סיומת `.xyz` → שגיאה | ✅ | רק `com/co.il/ac.il/org/net/gov.il/edu` |
| `test@gmail.com` → עובר | ✅ | |
| בלי תפקיד → שגיאה | ✅ | |
| username/מייל קיים → שגיאה מ-DB | ✅ | נבדק לפני OTP [:154](ClinicVetSystem/MainWindow.axaml.cs) |

### OTP — [MainWindow.axaml.cs:232](ClinicVetSystem/MainWindow.axaml.cs)
| מגיע מייל עם קוד | 👁 | תלוי Gmail SMTP (`clinic.vets.system@gmail.com`) — בדוק חי |
|---|:--:|---|
| קוד שגוי → שגיאה, לא נרשם | ✅ | |
| קוד נכון → נרשם + מספר עובד | ✅ | (כפוף לכך שה-Insert עובד) |
| חזרה אוטומטית להתחברות | ✅ | `ToggleSlide_Click` אחרי 3 שנ' |

### שכחתי סיסמה — [MainWindow.axaml.cs:286](ClinicVetSystem/MainWindow.axaml.cs)
| מייל לא קיים → שגיאה | ✅ | מייל קיים → OTP | ✅ |
|---|:--:|---|:--:|
| קוד נכון → מסך סיסמה חדשה | ✅ | לא עומד בתנאים → שגיאה | ✅ |
| אישור שונה → שגיאה | ✅ | הכל תקין → מתעדכן | ✅ |

> ⚠ **הערה צדדית:** הקובץ [RegisterWindow.axaml.cs](ClinicVetSystem/RegisterWindow.axaml.cs) הוא מסך הרשמה **ישן ולא בשימוש** — הוא שומר סיסמה **בלי הצפנה** ובלי OTP. המסך החי הוא MainWindow. כדאי למחוק אותו כדי שלא יתבלבלו.

---

## 👥 חלק 2 — ניהול לקוחות

### גישה — [MainMenuWindow.axaml.cs:32](ClinicVetSystem/MainMenuWindow.axaml.cs)
| מזכירה רואה "לקוחות" | ✅ | וטרינר **לא** רואה | ✅ |
|---|:--:|---|:--:|

### הוספת לקוח — [CustomersView.axaml.cs:384](ClinicVetSystem/CustomersView.axaml.cs)
| שם עם מספרים → שגיאה | ✅ | שם אותיות → עובר | ✅ |
|---|:--:|---|:--:|
| ת"ז ≠ 9 → שגיאה | ✅ | טלפון לא מתחיל ב-0 → שגיאה | ✅ |
| טלפון 8 ספרות → שגיאה | ✅ | `0501234567` → עובר | ✅ |
| מייל לא חוקי → שגיאה | ✅ | בלי מייל → עובר | ✅ |
| ת"ז קיימת → שגיאה | ✅ | זיהוי `duplicate/23505` | |

### חיפוש — [CustomersView.axaml.cs:67](ClinicVetSystem/CustomersView.axaml.cs)
| לפי ת"ז / טלפון / שם | ✅ | לא קיים → רשימה ריקה | ✅ |
|---|:--:|---|:--:|

### פרופיל לקוח
| פרופיל נפתח עם פרטים | ✅ | מוצגות כל החיות | ✅ |
|---|:--:|---|:--:|
| סטטוס חיסון (✓/⚠) | 👁 | לוגיקה ב-`NeedsVaccine` (>365 ימים) — ודא צבע/טקסט בעין |

### הוספת חיה מפרופיל לקוח — ❌ ראה **באג #3**
| שם עם מספרים → שגיאה | ❌ | חומק (אין בדיקת אותיות) |
|---|:--:|---|
| שם ריק → שגיאה | ✅ | זה כן נבדק |
| משקל 0 / 101 / 0.05 → שגיאה | ❌ | חומק (אין טווח) |
| תאריך עתידי / לפני 2000 → שגיאה | ❌ | חומק (אין בדיקה) |
| שבב 14/16 → שגיאה | ❌ | חומק (אין בדיקת 15 ספרות) |
| שמירה → חיה מופיעה | ❌ | חסום ע"י **באג #1** (DateOnly) |

---

## 🐾 חלק 3 — ניהול חיות (מסך ראשי)

### הוספת חיה — [PetsView.axaml.cs:169](ClinicVetSystem/PetsView.axaml.cs)
| שם עם מספרים → שגיאה | ✅ | משקל 0 / 100.1 → שגיאה | ✅ |
|---|:--:|---|:--:|
| משקל 0.1 (גבול) → עובר | ✅ | משקל 100 (גבול) → עובר | ✅ |
| תאריך עתידי → שגיאה | ✅ | 1999 → שגיאה | ✅ |
| 2000 (גבול) → עובר | ✅ | בלי בעלים → שגיאה | ✅ |
| שבב 14/16 → שגיאה | ✅ | **שמירה תקינה → חיה מופיעה** | ❌ **באג #1** |

> כל הוולידציות מושלמות — אבל השמירה עצמה נכשלת על DateOnly. **תיקון באג #1 פותח את כל החלק הזה.**

### חיפוש וסינון — [PetsView.axaml.cs:72](ClinicVetSystem/PetsView.axaml.cs)
| לפי שם / שבב | ✅ | סינון לפי סוג | ✅ |
|---|:--:|---|:--:|

### ניהול סוגי חיות — [PetsView.axaml.cs:299](ClinicVetSystem/PetsView.axaml.cs)
| הוספת סוג | ✅ | מחיקת סוג | ✅ |
|---|:--:|---|:--:|

---

## 📅 חלק 4 — יומן ותורים — [CalendarView.axaml.cs:526](ClinicVetSystem/CalendarView.axaml.cs)

| בחירת לקוח → מוצגות חיות שלו | ✅ | [:506](ClinicVetSystem/CalendarView.axaml.cs) |
|---|:--:|---|
| בחירת חיה+וטרינר+שעה+תאריך → שמירה | ✅ | שומר `Visit` (לא Pet → אין באג DateOnly) |
| תור בזמן שעבר → שגיאה | ✅ | [:547](ClinicVetSystem/CalendarView.axaml.cs) |
| פער < 30 דקות → שגיאה | ✅ | [:556](ClinicVetSystem/CalendarView.axaml.cs) — בודק מול **כל** התורים |
| תור נשמר → מופיע ביום הנכון | ✅ 👁 | קופץ ליום; ודא בעין |
| מחיקת תור → נעלם | ✅ | |

> 👁 **שים לב:** היומן מציג רק ימים א'–ה'. תור ביום ו'/שבת לא יוצג בלוח (לא בצ'קליסט).

---

## 🏥 חלק 5 — ביקורים וטיפולים — [VisitsView.axaml.cs](ClinicVetSystem/VisitsView.axaml.cs)

### גישה
| וטרינר → ניתן לערוך | ✅ | מזכירה → שדות נעולים + אין כפתור שמירה | ✅ [:300](ClinicVetSystem/VisitsView.axaml.cs) |
|---|:--:|---|:--:|

### בחירת ביקור והתראות — [:238](ClinicVetSystem/VisitsView.axaml.cs)
| לחיצה → פרטי מטופל | ✅ | אין תאריך חיסון → התראה | ✅ |
|---|:--:|---|:--:|
| חיסון > שנה → התראה צהובה | ✅ 👁 | לוגיקה `monthsSince>=12`; ודא צבע צהוב בעין |

### שמירת רשומה — [:437](ClinicVetSystem/VisitsView.axaml.cs)
| בלי סיבת ביקור → שגיאה | ✅ | בלי אבחנה → שגיאה | ✅ |
|---|:--:|---|:--:|
| תרופה + שמירה → עלות 150+מחיר | ✅ | שמירה → הודעת הצלחה | ✅ |
| מוצג "✓ טופל" (badge ירוק) | ✅ 👁 | כש-Diagnosis לא ריק |
| **מלאי יורד ב-1** | ❌ | **באג #2 — לא מומש** |

### היסטוריה
| לשונית היסטוריה / היום | ✅ | [:520](ClinicVetSystem/VisitsView.axaml.cs) |
|---|:--:|---|

---

## 💊 חלק 6 — מלאי תרופות — [InventoryView.axaml.cs](ClinicVetSystem/InventoryView.axaml.cs)

| הוספת תרופה (שם+מחיר+מלאי) | ✅ | עריכה | ✅ |
|---|:--:|---|:--:|
| מחיקה (חסומה אם בשימוש) | ✅ | `+` מעלה ב-1 | ✅ |
| `−` מוריד ב-1 | ✅ | `−` כשמלאי 0 → **disabled** | ✅ [:212](ClinicVetSystem/InventoryView.axaml.cs) |
| מלאי ≤ 5 → badge צהוב | ✅ 👁 | מלאי 0 → badge אדום "אזל" | ✅ 👁 |
| מלאי יורד אחרי ביקור | ❌ | **באג #2** |

---

## 📊 חלק 7 — Dashboard — [DashboardView.axaml.cs:21](ClinicVetSystem/DashboardView.axaml.cs)

| תורי היום | ✅ | סטטיסטיקות (סה"כ/ממתינים/טופלו) | ✅ |
|---|:--:|---|:--:|
| תרופות מלאי נמוך (≤10) | ✅ | אין תורים → מסך "הכל רגוע" | ✅ 👁 (✅ + טקסט) |

---

## 🔄 חלק 8 — אינטגרציה

| רשום→חיה→תור→ביקור→מלאי יורד | ❌ | נחסם ב-**באג #1** (חיה) ו-**באג #2** (מלאי) |
|---|:--:|---|
| לקוח חדש → בחיפוש Calendar | ✅ | |
| חיה → ברשימת חיות הלקוח | ❌ | נחסם ב-**באג #1** |
| ביקור נשמר → ב-Dashboard "טופל" | ✅ | |
| התנתק→כנס→נתונים נשמרו | ✅ | הכל ב-Supabase |

---

## 📌 סיכום

**הכל עובד חוץ מ-3 באגים:**
1. 🔴 **DateOnly** — חוסם כל הוספת חיה (חלקים 2,3,8). *זה מה שראיתם בהצגה.*
2. 🟠 **מלאי לא יורד** אחרי ביקור (חלקים 5,6,8).
3. 🟠 **ולידציה חסרה** בהוספת חיה מפרופיל לקוח (חלק 2).

**שורות שדורשות בדיקת-עין חיה (👁):** בעיקר צבעי badge, התראת חיסון צהובה, ושליחת מייל OTP — אלה דברים שאי אפשר לאמת מקוד בלבד.
</content>
</invoke>
